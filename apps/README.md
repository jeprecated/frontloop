# Applications

This directory groups frontloop applications:

- [`fl`](fl/) — Go CLI for managing frontloop queues from the terminal.
- [`pi-extension`](pi-extension/) — Pi extension package with `/fl-*` commands and frontloop agent tools.
