# Frontloop docs

- [Frontloop v2 epic layout specification](frontloop-v2-epic-layout.md) — implemented contract for `.frontloop/<epic>/<status>/<task>` queues, the `default` epic, archived epics, and legacy migration.

Historical planning documents under `docs/superpowers/` may describe the legacy flat queue layout used before v2. For current behavior, follow the v2 specification above.
